---
import { Image } from "astro:assets";

interface Props {
  img: ImageMetadata;
  calida?: string;
  alt: string;
  imgClass?: string;
}
const { img, calida = "high", alt, imgClass } = Astro.props;
---

<Image
  src={img}
  quality={calida}
  loading="lazy"
  format="avif"
  alt={alt}
  class={imgClass}
/>
